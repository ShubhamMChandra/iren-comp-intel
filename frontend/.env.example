# Railway API URL (no trailing slash). Set in Railway env for the frontend service.
NEXT_PUBLIC_API_URL=https://api-production-fdb3.up.railway.app

# Optional: enable verbose API logging in the browser console
# NEXT_PUBLIC_VERBOSE_API_LOG=1
